$(document).ready(function () {
    //$('.carousel').carousel({
    //    interval: 5000,
    //    pause: false
    //});

    $('.navbar-nav>li>a').on('click', function () {
        $('.navbar-collapse').collapse('hide');
    });
});

